{% extends 'home.html.twig' %}
{% block header %}<h3 class="page-title">Tenderat e mi</h3>{% endblock %}
{% block bodymain %}

    {% for message in app.flashes('success') %}
        <div class="flash-notice">
            {{ message }}
        </div>
    {% endfor %}
    <a href="{{ path('tender_krijo') }}"> <button  type="button" class="btn btn-warning">KRIJO TENDER TE RI</button></a>

    <div class="panel modifiko ">
    <h3>Tenderat Draft</h3>
    <table class="table  table-condensed bse-col-collapse-add">
        <thead>
        <tr>
            <th>TitullThirrje</th>
            <th>Vlera e Fondit Limit</th>
{#            <th>Veprime</th>#}
            <th>Modifiko</th>
            <th>Publiko</th>
            <th>Shiko</th>
            <th>Fshi</th>
        </tr>
        </thead>
        <tbody>
        {% for tender in tendersDraft %}
            <tr>

                <td>{{ tender.titullThirrje }}</td>
                <td>{{ tender.fondLimit }}</td>
                <td>
                    <a href="{{ path('tender_edit', {'id': tender.id}) }}">

                        <button type="button" class="btn btn-default"><i class="lnr lnr-pencil"></i></button></a>


                </td>
                <td>
                    <a href="{{ path('tender_publiko_draft',{ 'id': tender.id}) }}">
                        <button type="button" class="btn btn-success"><i class="lnr lnr-location"></i></button></a>

                </td>
                <td>
                    <a href="{{ path('tender_shiko', {'id': tender.id}) }}">

                        <button type="button" class="btn btn-info"><i class="fa fa-info-circle"></i></button></a>
                </td>
                <td>
                    <a href="{{path('tender_fshi', {'id': tender.id}) }}">
                        <button type="button" class="btn  btn-danger"><i class="lnr lnr-trash"></i></button></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="15">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
    <div class="panel modifiko">
 <h3> Tenderat Aktiv</h3>
    
    <table class="table table-condensed bse-col-collapse-add">
        <thead>
        <tr>
            <th>Id</th>
            <th>TitullThirrje</th>
            <th>FondLimit</th>
            <th>DataPerfundimit</th>
{#            <th>EmerStatusi</th>#}
            <th>Ofertat</th>
            <th>Modifiko</th>
            <th>Shiko</th>
            <th>Fshi</th>
            <th>Mbyll</th>

            {#           <th>BiznesIdId</th>#}
            {#            <th>TenderId</th>#}

        </tr>
        </thead>
        <tbody>
        {% for tender in tendersAktiv %}
            <tr>
            <td>{{ tender.id }}</td>
                <td>{{ tender.titullThirrje }}</td>
                <td>{{ tender.fondLimit }}</td>
                <td>{{ tender.dataPerfundimit ? tender.dataPerfundimit|date('Y-m-d H:i:s') : '' }}</td>
{#                <td>{{ tender.emerStatusi }}</td>#}
                <td><button type="button" class="btn btn-outline-dark">
                        <a href="{{ path('shiko_ofertat_tender_aktiv', {'id': tender.id}) }}">
                             {{ tender.nrAplikimesh }} <i class="lnr lnr-inbox"></i></a></button></td>
                <td>
                    <a href="{{ path('tender_modifiko_aktiv', {'id': tender.id}) }}">

                        <button type="button" class="btn btn-default"><i class="lnr lnr-pencil"></i></button></a>


                </td>
                <td>
                    <a href="{{ path('tender_shiko', {'id': tender.id}) }}">
                        <button type="button" class="btn btn-info"><i class="fa fa-info-circle"></i> </button></a>
                </td>
                <td>
                    <a href="{{ path('tender_fshi', {'id': tender.id}) }}">
                        <button type="button" class="btn btn-danger"><i class="lnr lnr-trash"></i> </button></a>
                </td>
                <td>
                    <a href="{{ path('tender_mbyll', {'id': tender.id}) }}">

                        <button type="button" class="btn btn-warning"><i class="lnr lnr-cross"></i></button></a>
                </td>
                <td>

                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="15">Asnjë e dhenë</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    </div>
<div class="panel modifiko">
    <h3>Tenderat Inaktiv</h3>
    <table class="table table-condensed bse-col-collapse-add">
        <thead>
        <tr>
            <th>Id</th>
            <th>TitullThirrje</th>
             <th>FondLimit</th>
             <th>DataPerfundimit</th>
             <th>EmerStatusi</th>
             <th>Ofertat</th>
            <th>Shiko</th>
            <th>Fshi</th>

        </tr>
        </thead>
        <tbody>
        {% for tender in tenderInaktiv %}
            <tr>
                <td>{{ tender.id }}</td>
                <td>{{ tender.titullThirrje }}</td>
                <td>{{ tender.fondLimit }}</td>
                <td>{{ tender.dataPerfundimit ? tender.dataPerfundimit|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ tender.emerStatusi }}</td>
                <td>
                    <button type="button" class="btn btn-outline-dark">
                        <a href="{{ path('shiko_ofertat_tender_aktiv', {'id': tender.id}) }}">
                            {{ tender.nrAplikimesh }} <i class="lnr lnr-inbox"></i></a></button>
                </td>
                <td>
                    <a href="{{ path('tender_shiko', {'id': tender.id}) }}">
                        <button type="button" class="btn btn-info"><i class="fa fa-info-circle"></i> </button></a>
                </td>
                <td><a href="{{ path('tender_fshi', {'id': tender.id}) }}">
                        <button type="button" class="btn btn-danger"><i class="lnr lnr-trash"></i></button>
                    </a>
                </td>
                {#                <td>{{ tender.biznesIdId }}</td>#}
                {#                <td>{{ tender.tenderId }}</td>#}
                <td>
                    {#                    <a href="{{ path('tender_show', {'id': tender.id}) }}">show</a>#}
                    {#                    <a href="{{ path('tender_edit', {'id': tender.id}) }}">edit</a>#}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="15">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
{#    <a href="{{ path('tender_new') }}">Create new</a>#}
{% endblock %}

