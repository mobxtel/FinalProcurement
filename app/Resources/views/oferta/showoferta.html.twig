{% extends 'home.html.twig' %}
        {% block bodymain %}
            {{ form_start(form) }}
            {{ form_widget(form) }}


            <button type="submit" class="btn btn-primary">{{ button_label|default('Save') }}

{#            {{ form_row(form.fshij,path('fshiOferten',{'id':oferte.id}) ) }}#}
            {{ form_end(form) }}

        {% endblock %}
        {% block javascripts %}{% endblock %}
